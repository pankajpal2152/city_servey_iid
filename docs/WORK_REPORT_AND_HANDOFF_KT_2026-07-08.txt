# Work Report and Backend Handoff KT

Date: 2026-07-09

## Work Completed Today

Implemented and verified the current City Survey backend API scope in the
existing Node.js application.

Completed items:

- Integrated `DEV_CITY_SURVEY` RDS database credentials through `.env`.
- Kept database config source-driven by environment variables in `config/db.js`.
- Implemented active authenticate API:
  - `POST /api/auth/api-post-authenticate-user`
  - Procedure: `USP_POST_USER_AUTHENTICATE_ACTIVITY`
- Implemented active add/update user API:
  - `POST /api/user/api-post-add-update-user`
  - Procedure: `USP_POST_USER_PROFILE_ACTIVITY`
- Implemented active add/update customer details API:
  - `POST /api/customer/api-post-add-update-customer-details`
  - Procedure: `USP_POST_CUSTOMER_DETAILS_ACTIVITY`
- Implemented active customer list details API:
  - `GET /api/customer/api-get-view-list-customer-details`
  - Procedure: `USP_GET_LIST_CUSTOMER_ACTIVITY`
- Implemented active add/update project API:
  - `POST /api/project/api-post-add-update-project`
  - Procedure: `USP_POST_PROJECT_ACTIVITY`
- Implemented active project list details API:
  - `GET /api/project/api-get-view-list-project-details`
  - Procedure: `USP_GET_LIST_PROJECT_ACTIVITY`
- Merged senior developer health API:
  - `GET /health`
  - Route: `routes/health.js`
- Added JWT generation for successful authentication.
- Added JWT protection for add/update user and customer details.
- Added JWT protection for add/update project.
- Added JWT protection for project list details.
- Added response helper for stored procedure `JSON_VALUE` parsing.
- Disabled active mounting of old event, attendee, payment, file upload, and
  email APIs from `index.js`.
- Limited Swagger generation to the active City Survey route files.
- Updated Swagger server URL to relative `/` so Swagger calls the current
  running port instead of any hardcoded localhost port.
- Added startup handling for `EADDRINUSE` port conflicts.
- Updated README and added this documentation pack.

## Important Technical Finding

The database developer's sample for `USP_POST_USER_PROFILE_ACTIVITY` showed a
plain JSON object, but the live stored procedure reads fields using `$[0]`.

Backend decision:

- Frontend can send a normal object.
- Backend wraps the object as a one-item array before calling the stored
  procedure.

Implemented in:

- `routes/user.js`

## Files Changed or Added

Backend files:

- `config/db.js`
- `index.js`
- `middlewares/auth.js`
- `routes/auth.js`
- `routes/user.js`
- `routes/customer.js`
- `routes/project.js`
- `routes/health.js`
- `utils/procedureResponse.js`
- `docs/swagger.js`
- `config/test.js`

Documentation files:

- `README.md`
- `docs/INDEX.md`
- `docs/MODULE_ARCHITECTURE_FLOW.md`
- `docs/BROWSER_DEMO_AND_DEMO_FLOW.md`
- `docs/DEV_SERVER_DEPLOYMENT_CHECKLIST.md`
- `docs/RUN_AND_TEST_GUIDE.md`
- `docs/API_URLS.md`
- `docs/API_ENDPOINTS_DEV_SERVER.txt`
- `docs/WORK_REPORT_AND_HANDOFF_KT_2026-07-08.md`
- `docs/UI_DEVELOPER_HANDOFF_KT.md`

## Verification Done

Syntax checks:

- `node --check index.js`
- `node --check routes/auth.js`
- `node --check routes/user.js`
- `node --check routes/customer.js`
- `node --check routes/project.js`
- `node --check routes/health.js`
- `node --check middlewares/auth.js`
- `node --check utils/procedureResponse.js`
- `node --check docs/swagger.js`

Database checks:

- Connected to `DEV_CITY_SURVEY`
- Confirmed procedure exists:
  - `USP_POST_USER_AUTHENTICATE_ACTIVITY`
  - `USP_POST_USER_PROFILE_ACTIVITY`
  - `USP_GET_LIST_CUSTOMER_ACTIVITY`
  - `USP_POST_CUSTOMER_DETAILS_ACTIVITY`
  - `USP_GET_LIST_PROJECT_ACTIVITY`
  - `USP_POST_PROJECT_ACTIVITY`
- Auth procedure returned `status: true` for sample user.

HTTP API smoke test:

- Started local server on configured local port `3103`
- Health API returns HTTP 200 without JWT
- Auth API returned HTTP 200 and token
- Protected add/update API accepted token
- Customer list API returns read-only customer data with nested `PIC_DETAILS`
- Existing sample email returned `This Email Id Already Exists`
- Sample email count stayed `1 -> 1`, so no duplicate test user was created
- Customer route validation can be tested safely with `{ "ITEM": "ADD" }`;
  full customer samples are write operations.
- Project route validation can be tested safely with `{ "ITEM": "ADD" }`;
  full project samples are write operations.
- Project list route can be tested safely with `ITEM=VIEW_ALL`; it is a
  read-only API.

## Current Working API Contracts

### Authenticate User

Endpoint:

`POST /api/auth/api-post-authenticate-user`

Request:

```json
[
  {
    "USER_NAME": "sai@yopmail.com",
    "PASSWORD": "Abc@1234"
  }
]
```

Response:

- Returns DB user data
- Returns `Token` when `status` is `true`

### Add or Update User

Endpoint:

`POST /api/user/api-post-add-update-user`

Header:

```text
Authorization: Bearer <Token>
```

Request:

```json
{
  "ITEM": "ADD_USER",
  "USER_SYS_ID": 0,
  "SYSTEM_ROLE_SYS_ID": 1,
  "FIRST_NAME": "Sai",
  "LAST_NAME": "Roy",
  "GENDER": "Male",
  "MOBILE_NO": "7894561230",
  "EMAIL_ID": "sai@yopmail.com",
  "ORGANISATION_SYS_ID": "1",
  "PASSWORD": "Abc@1234",
  "CREATED_BY": "1"
}
```

### Add or Update Customer Details

Endpoint:

`POST /api/customer/api-post-add-update-customer-details`

Header:

```text
Authorization: Bearer <Token>
```

Request:

```json
{
  "ITEM": "ADD",
  "CUSTOMER_TYPE": "Individual",
  "CUSTOMER_NAME": "Ravi",
  "BILLING_ADDRESS": "Kolkata",
  "CREATED_BY": 1,
  "PIC": [
    {
      "FULL_NAME": "Ranjan",
      "CONTACT_NO": "7875585",
      "EMAIL_ID": "ranjan@gmail.com",
      "DEPARTMENT": "Back Office",
      "DESIGNATION": "Employee"
    }
  ]
}
```

### View Customer List Details

Endpoint:

`GET /api/customer/api-get-view-list-customer-details`

Header:

```text
Authorization: Bearer <Token>
```

Stored procedure call:

```sql
CALL USP_GET_LIST_CUSTOMER_ACTIVITY('VIEW_CUSTOMER', 'VIEW_ALL', @ERRNO, @ERRMSG);
```

Default query:

```text
ITEM=VIEW_ALL
```

### Add or Update Project

Endpoint:

`POST /api/project/api-post-add-update-project`

Header:

```text
Authorization: Bearer <Token>
```

Request:

```json
{
  "ITEM": "ADD",
  "CUSTOMER_SYS_ID": 4,
  "PIC_SYS_ID": 5,
  "SITE_ADDRESS": "Mumbai",
  "ASSIGNED_SURVEYOR": "Marcus Koh",
  "TYPE_OF_SURVEY": "MRT Pier",
  "TYPE_OF_PROPERTY": "Own",
  "CREATED_BY": 1
}
```

### View Project List Details

Endpoint:

`GET /api/project/api-get-view-list-project-details`

Header:

```text
Authorization: Bearer <Token>
```

Stored procedure call:

```sql
CALL USP_GET_LIST_PROJECT_ACTIVITY('VIEW_PROJECT', 'VIEW_ALL', @ERRNO, @ERRMSG);
```

Default query:

```text
ITEM=VIEW_ALL
```

### Health Check

Endpoint:

`GET /health`

Response:

```json
{
  "status": "true",
  "response": "City Survey API is running",
  "uptime": 1.23,
  "timestamp": "2026-07-09T00:00:00.000Z"
}
```

## Handoff KT for Backend Developer

Walkthrough order:

1. Start at `index.js`.
2. Show only active City Survey routes are mounted.
3. Show `config/db.js` reads all DB config from `.env`.
4. Show `routes/auth.js`:
   - normalizes auth payload
   - calls auth procedure
   - parses `JSON_VALUE`
   - signs JWT
5. Show `middlewares/auth.js`:
   - expects `Authorization: Bearer <token>`
   - returns 401 for missing token
   - returns 403 for invalid token
6. Show `routes/user.js`:
   - requires JWT middleware
   - validates `ITEM`
   - wraps payload into array for DB
   - calls user profile procedure
7. Show `routes/customer.js`:
   - requires JWT middleware
   - calls customer list procedure for view/list
   - validates `ITEM` and required `ADD` fields
   - calls customer details procedure
8. Show `routes/project.js`:
   - requires JWT middleware
   - calls project list procedure for view/list
   - validates `ITEM` and required `ADD` fields
   - calls project procedure
9. Show `routes/health.js`:
   - returns API process status without DB or JWT
10. Show `utils/procedureResponse.js`:
   - parses procedure response safely
11. Show Swagger at `/api/api-docs`.
12. Run health, auth, user, customer list, project list, safe customer
    validation, and safe project validation demo.

## Handoff KT for Database Developer

Confirmed:

- Backend connects to `DEV_CITY_SURVEY`.
- Backend calls stored procedures using parameterized `conn.execute`.
- Backend uses operation names exactly as provided:
  - `AUTHENTICATE_USER`
  - `ADD_UPDATE_USER`
  - `VIEW_CUSTOMER`
  - `ADD_UPDATE_CUSTOMER`
  - `VIEW_PROJECT`
  - `ADD_UPDATE_PROJECT`

DB follow-up points:

- Confirm final JSON shape for `USP_POST_USER_PROFILE_ACTIVITY`. Current live
  procedure uses `$[0]`, so backend sends array shape to DB.
- Confirm update-user payload fields for `ITEM: UPDATE_USER`.
- Confirm whether password should be mandatory for update.
- Confirm whether future read/list user APIs will be provided.
- Confirm final update/delete customer payload fields and expected responses.
- Confirm final update/delete project payload fields and expected responses.
- Confirm standard response shape for success and failure across procedures.

## Known Notes and Risks

- Port `3000` was already occupied locally during verification, so temporary
  testing was moved to local port `3103`.
- The stored procedure commits internally, so write tests create real dev DB
  records.
- There are no automated unit/integration test files yet.
- Legacy route files still exist in the repo, but they are not mounted.

## Recommended Next Backend Tasks

- Add automated monitoring that calls `GET /health` during deployments.
- Add automated integration test script under a `tests` or `scripts` folder.
- Confirm update-user and delete-user DB contracts.
- Confirm update/delete customer DB contracts.
- Confirm update/delete project DB contracts.
- Remove or archive legacy route files after senior developer approval.
- Keep `.env.example` updated and ensure `.env` stays ignored before pushing to
  remote.
