# Module Architecture and Scenario Flow

Date: 2026-07-15

## Current Backend Scope

The City Survey backend currently exposes the following active APIs:

- Authenticate a user through `USP_POST_USER_AUTHENTICATE_ACTIVITY`
- View list user info through `USP_GET_USER_LIST_ACTIVITY`
- Add or update a user through `USP_POST_USER_PROFILE_ACTIVITY`
- Add or update customer details through `USP_POST_CUSTOMER_DETAILS_ACTIVITY`
- View list customer details through `USP_GET_LIST_CUSTOMER_ACTIVITY`
- View specific customer profile/details through `USP_GET_SPECIFIC_CUSTOMER_ACTIVITY`
- View list project details through `USP_GET_LIST_PROJECT_ACTIVITY`
- View specific project details through `USP_GET_SPECIFIC_PROJECT_ACTIVITY`
- Add or update project through `USP_POST_PROJECT_ACTIVITY`
- View master system roles through `USP_GET_ALL_MASTER_DATA`
- Add or update master system role through `USP_POST_ALL_MASTER_DATA`
- View master survey type through `USP_GET_ALL_MASTER_DATA`
- Add or update master survey type through `USP_POST_ALL_MASTER_DATA`
- View master property type through `USP_GET_ALL_MASTER_DATA`
- Add or update master property type through `USP_POST_ALL_MASTER_DATA`
- View master details through `USP_GET_ALL_MASTER_DATA`
- Check API process health through `GET /health`

All other legacy event, attendee, payment, file upload, and email modules are
not mounted in `index.js` for this City Survey phase.

## Module Map

| Module | File | Responsibility | Current Status |
| --- | --- | --- | --- |
| App entry | `index.js` | Creates Express app, configures JSON parsing, CORS, routes, Swagger, and server port | Active |
| Database pool | `config/db.js` | Creates MySQL pool from `.env` values | Active |
| Auth route | `routes/auth.js` | Handles user authentication, calls auth stored procedure, signs JWT | Active |
| User route | `routes/user.js` | Handles user list view plus add/update user, calls user stored procedures | Active |
| Customer route | `routes/customer.js` | Handles customer list view, specific customer profile, add/update customer details, and PIC records | Active |
| Project route | `routes/project.js` | Handles project list view, specific project details, and add/update project, calls project stored procedures | Active |
| Master route | `routes/master.js` | Handles system-role, survey-type, property-type, and master-details list view plus add/update APIs | Active |
| Health route | `routes/health.js` | Returns a lightweight API status response without JWT or DB calls | Active |
| JWT middleware | `middlewares/auth.js` | Validates `Authorization: Bearer <token>` before protected APIs | Active |
| Procedure helper | `utils/procedureResponse.js` | Normalizes stored procedure `JSON_VALUE` responses | Active |
| Swagger config | `docs/swagger.js` | Builds Swagger docs only for active City Survey routes | Active |
| Legacy routes | `routes/event.js`, `routes/payment.js`, and others | Old iideas/event application APIs | Not mounted |

## Current Runtime Configuration

- Local configured port: `3103`
- Local Swagger URL: `http://localhost:3103/api/api-docs`
- Swagger server URL: `/`
- Port conflict handling: `index.js` catches `EADDRINUSE` and prints a clear
  message asking the developer to stop the existing process or update `PORT`.

Swagger intentionally uses a relative server URL. This prevents the Swagger UI
from calling old hardcoded ports such as `3000` when the backend is running on
`3103` or another configured port.

## High Level Architecture

```mermaid
flowchart LR
  UI["Browser or UI Application"]
  Express["Express App - index.js"]
  AuthRoute["Auth Route - routes/auth.js"]
  UserRoute["User Route - routes/user.js"]
  CustomerRoute["Customer Route - routes/customer.js"]
  ProjectRoute["Project Route - routes/project.js"]
  MasterRoute["Master Route - routes/master.js"]
  HealthRoute["Health Route - routes/health.js"]
  Jwt["JWT Middleware - middlewares/auth.js"]
  Helper["Procedure Response Helper"]
  Pool["MySQL Pool - config/db.js"]
  DB["RDS MySQL - DEV_CITY_SURVEY"]
  AuthSP["USP_POST_USER_AUTHENTICATE_ACTIVITY"]
  UserListSP["USP_GET_USER_LIST_ACTIVITY"]
  UserSP["USP_POST_USER_PROFILE_ACTIVITY"]
  CustomerSP["USP_POST_CUSTOMER_DETAILS_ACTIVITY"]
  CustomerListSP["USP_GET_LIST_CUSTOMER_ACTIVITY"]
  SpecificCustomerSP["USP_GET_SPECIFIC_CUSTOMER_ACTIVITY"]
  ProjectListSP["USP_GET_LIST_PROJECT_ACTIVITY"]
  SpecificProjectSP["USP_GET_SPECIFIC_PROJECT_ACTIVITY"]
  ProjectSP["USP_POST_PROJECT_ACTIVITY"]
  MasterListSP["USP_GET_ALL_MASTER_DATA"]
  MasterPostSP["USP_POST_ALL_MASTER_DATA"]

  UI --> Express
  Express --> AuthRoute
  Express --> UserRoute
  Express --> CustomerRoute
  Express --> ProjectRoute
  Express --> MasterRoute
  Express --> HealthRoute
  UserRoute --> Jwt
  CustomerRoute --> Jwt
  ProjectRoute --> Jwt
  MasterRoute --> Jwt
  AuthRoute --> Pool
  UserRoute --> Pool
  CustomerRoute --> Pool
  ProjectRoute --> Pool
  MasterRoute --> Pool
  Pool --> DB
  DB --> AuthSP
  DB --> UserListSP
  DB --> UserSP
  DB --> CustomerSP
  DB --> CustomerListSP
  DB --> SpecificCustomerSP
  DB --> ProjectListSP
  DB --> SpecificProjectSP
  DB --> ProjectSP
  DB --> MasterListSP
  DB --> MasterPostSP
  AuthSP --> Helper
  UserListSP --> Helper
  UserSP --> Helper
  CustomerSP --> Helper
  CustomerListSP --> Helper
  SpecificCustomerSP --> Helper
  ProjectListSP --> Helper
  SpecificProjectSP --> Helper
  ProjectSP --> Helper
  MasterListSP --> Helper
  MasterPostSP --> Helper
  Helper --> AuthRoute
  Helper --> UserRoute
  Helper --> CustomerRoute
  Helper --> ProjectRoute
  Helper --> MasterRoute
```

## Request Flow: Health Check

Endpoint:

`GET /health`

```mermaid
sequenceDiagram
  participant Client as Browser, Load Balancer, or DevOps Probe
  participant API as Express Health API

  Client->>API: GET /health
  API-->>Client: status, response, uptime, timestamp
```

The health endpoint does not require JWT and does not call the database. It is
intended for deployment checks, local smoke tests, and process monitoring.

## Request Flow: Authenticate User

Endpoint:

`POST /api/auth/api-post-authenticate-user`

Stored procedure:

`USP_POST_USER_AUTHENTICATE_ACTIVITY('AUTHENTICATE_USER', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Auth API
  participant DB as DEV_CITY_SURVEY
  participant SP as Auth Stored Procedure

  UI->>API: POST USER_NAME and PASSWORD
  API->>API: Normalize object or array payload
  API->>DB: CALL USP_POST_USER_AUTHENTICATE_ACTIVITY
  DB->>SP: Validate credentials
  SP-->>DB: JSON_VALUE response
  DB-->>API: User data and status
  API->>API: Sign JWT when status is true
  API-->>UI: Token plus DB response
```

### Auth Payload Accepted by API

Array format:

```json
[
  {
    "USER_NAME": "sai@yopmail.com",
    "PASSWORD": "Abc@1234"
  }
]
```

Object format also works:

```json
{
  "USER_NAME": "sai@yopmail.com",
  "PASSWORD": "Abc@1234"
}
```

The API also maps older frontend-style keys:

- `username` to `USER_NAME`
- `email` to `USER_NAME`
- `EMAIL_ID` to `USER_NAME`
- `password` to `PASSWORD`

## Request Flow: View User List Info

Endpoint:

`GET /api/user/api-get-view-user-list-info`

Stored procedure:

`USP_GET_USER_LIST_ACTIVITY('VIEW_USER', P_ITEM, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express User API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as User List Stored Procedure

  UI->>API: GET user list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>DB: CALL USP_GET_USER_LIST_ACTIVITY
  DB->>SP: Fetch user list details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: User list JSON
```

Default query behavior:

```text
GET /api/user/api-get-view-user-list-info
```

is treated as:

```text
GET /api/user/api-get-view-user-list-info?ITEM=VIEW_ALL
```

## Request Flow: Add or Update User

Endpoint:

`POST /api/user/api-post-add-update-user`

Stored procedure:

`USP_POST_USER_PROFILE_ACTIVITY('ADD_UPDATE_USER', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express User API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as User Profile Stored Procedure

  UI->>API: POST user payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and wrap object as one-item array
  API->>DB: CALL USP_POST_USER_PROFILE_ACTIVITY
  DB->>SP: Add/update user logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### Important DB Shape Note

The DB handoff comment showed a plain object JSON example. The live stored
procedure currently extracts fields using `$[0]`, for example `$[0].ITEM` and
`$[0].USER_SYS_ID`.

Because of that, `routes/user.js` accepts a normal object from the frontend but
sends the stored procedure a one-item array:

```json
[
  {
    "ITEM": "ADD_USER",
    "USER_SYS_ID": 0
  }
]
```

This keeps the UI simple while matching the database implementation.

## Request Flow: Add or Update Customer Details

Endpoint:

`POST /api/customer/api-post-add-update-customer-details`

Stored procedure:

`USP_POST_CUSTOMER_DETAILS_ACTIVITY('ADD_UPDATE_CUSTOMER', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Customer API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Customer Details Stored Procedure

  UI->>API: POST customer payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and required ADD fields
  API->>DB: CALL USP_POST_CUSTOMER_DETAILS_ACTIVITY
  DB->>SP: Add/update customer and PIC logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### Customer Payload Shape

The API accepts the object shape provided by the database developer:

```json
{
  "ITEM": "ADD",
  "CUSTOMER_TYPE": "Individual",
  "CUSTOMER_NAME": "Ravi",
  "BILLING_ADDRESS": "Kolkata",
  "CREATED_BY": 1,
  "PIC": [
    {
      "FULL_NAME": "Ranjan",
      "CONTACT_NO": "7875585",
      "EMAIL_ID": "ranjan@gmail.com",
      "DEPARTMENT": "Back Office",
      "DESIGNATION": "Employee"
    }
  ]
}
```

If a client accidentally sends a one-item array, the backend unwraps the first
object before calling the stored procedure. This keeps `PIC` available at the
root object path expected by the live procedure.

## Request Flow: View Customer List Details

Endpoint:

`GET /api/customer/api-get-view-list-customer-details`

Stored procedure:

`USP_GET_LIST_CUSTOMER_ACTIVITY('VIEW_CUSTOMER', P_ITEM, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Customer API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Customer List Stored Procedure

  UI->>API: GET customer list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>DB: CALL USP_GET_LIST_CUSTOMER_ACTIVITY
  DB->>SP: Fetch customer list and PIC details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Customer list JSON
```

Default query behavior:

```text
GET /api/customer/api-get-view-list-customer-details
```

is treated as:

```text
GET /api/customer/api-get-view-list-customer-details?ITEM=VIEW_ALL
```

## Request Flow: View Specific Customer Details

Endpoint:

`GET /api/customer/api-get-view-specific-customer-details`

Stored procedure:

`USP_GET_SPECIFIC_CUSTOMER_ACTIVITY('VIEW_CUSTOMER', P_ITEM, P_CUSTOMER_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Customer API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Specific Customer Stored Procedure

  UI->>API: GET customer profile with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default PROFILE
  API->>API: Validate CUSTOMER_SYS_ID
  API->>DB: CALL USP_GET_SPECIFIC_CUSTOMER_ACTIVITY
  DB->>SP: Fetch customer profile/details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Customer profile/details JSON
```

Default query behavior:

```text
GET /api/customer/api-get-view-specific-customer-details?CUSTOMER_SYS_ID=4
```

is treated as:

```text
GET /api/customer/api-get-view-specific-customer-details?ITEM=PROFILE&CUSTOMER_SYS_ID=4
```

## Request Flow: View Project List Details

Endpoint:

`GET /api/project/api-get-view-list-project-details`

Stored procedure:

`USP_GET_LIST_PROJECT_ACTIVITY('VIEW_PROJECT', P_ITEM, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Project API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Project List Stored Procedure

  UI->>API: GET project list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>DB: CALL USP_GET_LIST_PROJECT_ACTIVITY
  DB->>SP: Fetch project list details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Project list JSON
```

Default query behavior:

```text
GET /api/project/api-get-view-list-project-details
```

is treated as:

```text
GET /api/project/api-get-view-list-project-details?ITEM=VIEW_ALL
```

## Request Flow: View Specific Project Details

Endpoint:

`GET /api/project/api-get-view-specific-project-details`

Stored procedure:

`USP_GET_SPECIFIC_PROJECT_ACTIVITY('VIEW_PROJECT', P_ITEM, P_PROJECT_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Project API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Specific Project Stored Procedure

  UI->>API: GET project details with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate PROJECT_SYS_ID and default ITEM to SPECIFIC
  API->>DB: CALL USP_GET_SPECIFIC_PROJECT_ACTIVITY
  DB->>SP: Fetch specific project details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Specific project JSON
```

Default query behavior:

```text
GET /api/project/api-get-view-specific-project-details?PROJECT_SYS_ID=2
```

is treated as:

```text
GET /api/project/api-get-view-specific-project-details?ITEM=SPECIFIC&PROJECT_SYS_ID=2
```

## Request Flow: Add or Update Project

Endpoint:

`POST /api/project/api-post-add-update-project`

Stored procedure:

`USP_POST_PROJECT_ACTIVITY('ADD_UPDATE_PROJECT', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Project API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Project Stored Procedure

  UI->>API: POST project payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and required ADD fields
  API->>DB: CALL USP_POST_PROJECT_ACTIVITY
  DB->>SP: Add/update project logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### Project Payload Shape

The API accepts the object shape provided by the database developer:

```json
{
  "ITEM": "ADD",
  "CUSTOMER_SYS_ID": 17,
  "PIC_SYS_ID": 22,
  "SITE_ADDRESS": "51 Jalan Kukoh 01-37, 1620051, Singapore",
  "ASSIGNED_SURVEYOR_SYS_ID": 1,
  "ASSIGNED_SURVEYOR": "Marcus Koh",
  "SURVEY_TYPE_SYS_ID": 5,
  "PROPERTY_TYPE_SYS_ID": 15,
  "CREATED_BY": 1
}
```

If a client accidentally sends a one-item array, the backend unwraps the first
object before calling the stored procedure.

## Request Flow: View Master System Roles

Endpoint:

`GET /api/master/api-get-view-master-system-roles`

Stored procedure:

`USP_GET_ALL_MASTER_DATA('VIEW_SYSTEM_ROLE', P_ITEM, P_RECORD_SYS_ID, P_ORGANIZATION_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: GET system role list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>API: Use RECORD_SYS_ID and ORGANIZATION_SYS_ID query values or default 0
  API->>DB: CALL USP_GET_ALL_MASTER_DATA
  DB->>SP: Fetch system role master data
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: System role list JSON
```

Default query behavior:

```text
GET /api/master/api-get-view-master-system-roles
```

is treated as:

```text
GET /api/master/api-get-view-master-system-roles?ITEM=VIEW_ALL&RECORD_SYS_ID=0&ORGANIZATION_SYS_ID=0
```

Missing `RECORD_SYS_ID` and `ORGANIZATION_SYS_ID` values are sent as `0`.

## Request Flow: Add or Update Master System Role

Endpoint:

`POST /api/master/api-post-add-update-master-system-role`

Stored procedure:

`USP_POST_ALL_MASTER_DATA('ADD_UPDATE_SYSTEM_ROLE', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: POST system role payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and required ADD fields
  API->>DB: CALL USP_POST_ALL_MASTER_DATA
  DB->>SP: Add/update system role logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### System Role Payload Shape

The API accepts the object shape provided by the database developer:

```json
{
  "ITEM": "ADD",
  "RECORD_SYS_ID": "0",
  "SYSTEM_ROLE": "Business Admin",
  "ROLE_DESC": "Business Admin",
  "USER_TYPE": "Internal",
  "CREATED_BY": 1
}
```

If a client accidentally sends a one-item array, the backend unwraps the first
object before calling the stored procedure.

## Request Flow: View Master Survey Type

Endpoint:

`GET /api/master/api-get-view-master-survey-type`

Stored procedure:

`USP_GET_ALL_MASTER_DATA('VIEW_SURVEY_TYPE', P_ITEM, P_RECORD_SYS_ID, P_ORGANIZATION_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: GET survey type list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>API: Use RECORD_SYS_ID and ORGANIZATION_SYS_ID query values or default 0
  API->>DB: CALL USP_GET_ALL_MASTER_DATA
  DB->>SP: Fetch survey type master data
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Survey type list JSON
```

Default query behavior:

```text
GET /api/master/api-get-view-master-survey-type
```

is treated as:

```text
GET /api/master/api-get-view-master-survey-type?ITEM=VIEW_ALL&RECORD_SYS_ID=0&ORGANIZATION_SYS_ID=0
```

## Request Flow: Add or Update Master Survey Type

Endpoint:

`POST /api/master/api-post-add-update-master-survey-type`

Stored procedure:

`USP_POST_ALL_MASTER_DATA('ADD_UPDATE_SURVEY_TYPE', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: POST survey type payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and required ADD fields
  API->>DB: CALL USP_POST_ALL_MASTER_DATA
  DB->>SP: Add/update survey type logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### Survey Type Payload Shape

The API accepts the object shape provided by the database developer:

```json
{
  "ITEM": "ADD",
  "RECORD_SYS_ID": "0",
  "SURVEY_TYPE": "Seminer",
  "CREATED_BY": "1"
}
```

If a client accidentally sends a one-item array, the backend unwraps the first
object before calling the stored procedure.

## Request Flow: View Master Property Type

Endpoint:

`GET /api/master/api-get-view-master-property-type`

Stored procedure:

`USP_GET_ALL_MASTER_DATA('VIEW_PROPERTY_TYPE', P_ITEM, P_RECORD_SYS_ID, P_ORGANIZATION_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: GET property type list with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>API: Use RECORD_SYS_ID and ORGANIZATION_SYS_ID query values or default 0
  API->>DB: CALL USP_GET_ALL_MASTER_DATA
  DB->>SP: Fetch property type master data
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Property type list JSON
```

Default query behavior:

```text
GET /api/master/api-get-view-master-property-type
```

is treated as:

```text
GET /api/master/api-get-view-master-property-type?ITEM=VIEW_ALL&RECORD_SYS_ID=0&ORGANIZATION_SYS_ID=0
```

## Request Flow: Add or Update Master Property Type

Endpoint:

`POST /api/master/api-post-add-update-master-property-type`

Stored procedure:

`USP_POST_ALL_MASTER_DATA('ADD_UPDATE_PROPERTY_TYPE', P_JSON, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: POST property type payload with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Validate ITEM and required ADD fields
  API->>DB: CALL USP_POST_ALL_MASTER_DATA
  DB->>SP: Add/update property type logic
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Procedure response JSON
```

### Property Type Payload Shape

The API accepts the object shape provided by the database developer:

```json
{
  "ITEM": "ADD",
  "RECORD_SYS_ID": "0",
  "PROPERTY_TYPE": "Seminer",
  "CREATED_BY": "1"
}
```

If a client accidentally sends a one-item array, the backend unwraps the first
object before calling the stored procedure.

## Request Flow: View Master Details

Endpoint:

`GET /api/master/api-get-view-master-details`

Stored procedure:

`USP_GET_ALL_MASTER_DATA('VIEW_MASTER_NAME', P_ITEM, P_RECORD_SYS_ID, P_ORGANIZATION_SYS_ID, @ERRNO, @ERRMSG)`

```mermaid
sequenceDiagram
  participant UI as Browser or UI
  participant API as Express Master API
  participant JWT as JWT Middleware
  participant DB as DEV_CITY_SURVEY
  participant SP as Master Data Stored Procedure

  UI->>API: GET master details with Bearer token
  API->>JWT: Validate JWT
  JWT-->>API: Attach req.user
  API->>API: Use ITEM query value or default VIEW_ALL
  API->>API: Use RECORD_SYS_ID and ORGANIZATION_SYS_ID query values or default 0
  API->>DB: CALL USP_GET_ALL_MASTER_DATA
  DB->>SP: Fetch master details
  SP-->>DB: JSON_VALUE response
  DB-->>API: Procedure response
  API-->>UI: Master details JSON
```

Default query behavior:

```text
GET /api/master/api-get-view-master-details
```

is treated as:

```text
GET /api/master/api-get-view-master-details?ITEM=VIEW_ALL&RECORD_SYS_ID=0&ORGANIZATION_SYS_ID=0
```

## Scenario Matrix

| Scenario | API | Input | Expected Result |
| --- | --- | --- | --- |
| Successful login | `POST /api/auth/api-post-authenticate-user` | Valid `USER_NAME` and `PASSWORD` | HTTP 200, `Token`, `status: true` |
| Missing login fields | `POST /api/auth/api-post-authenticate-user` | `{}` | HTTP 400, `USER_NAME and PASSWORD are required` |
| Invalid credentials | `POST /api/auth/api-post-authenticate-user` | Wrong password | HTTP 401 with DB response |
| View user list | `GET /api/user/api-get-view-user-list-info` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, user list response |
| Add existing user | `POST /api/user/api-post-add-update-user` | Existing `EMAIL_ID`, valid token | HTTP 200, DB response such as `This Email Id Already Exists` |
| Add new user | `POST /api/user/api-post-add-update-user` | Unique `EMAIL_ID`, valid token | HTTP 200, DB success response, new dev DB row |
| Missing bearer token | `POST /api/user/api-post-add-update-user` | No `Authorization` header | HTTP 401 |
| Invalid bearer token | `POST /api/user/api-post-add-update-user` | Bad token | HTTP 403 |
| Missing `ITEM` | `POST /api/user/api-post-add-update-user` | `{}` with token | HTTP 400, `ITEM is required` |
| View customer list | `GET /api/customer/api-get-view-list-customer-details` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, customer list with nested `PIC_DETAILS` |
| View specific customer details | `GET /api/customer/api-get-view-specific-customer-details` | Valid token, `CUSTOMER_SYS_ID`, optional `ITEM=PROFILE` | HTTP 200, customer profile/details response |
| Add customer details | `POST /api/customer/api-post-add-update-customer-details` | Valid customer payload, valid token | HTTP 200, DB response, customer/PIC records may be created |
| Missing customer fields | `POST /api/customer/api-post-add-update-customer-details` | Missing required ADD field, valid token | HTTP 400 with validation response |
| View project list | `GET /api/project/api-get-view-list-project-details` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, project list response |
| View specific project | `GET /api/project/api-get-view-specific-project-details` | Valid token, `PROJECT_SYS_ID=2`, optional `ITEM=SPECIFIC` | HTTP 200, specific project response |
| Add project | `POST /api/project/api-post-add-update-project` | Valid project payload, valid token | HTTP 200, DB response, project records may be created |
| Missing project fields | `POST /api/project/api-post-add-update-project` | Missing required ADD field, valid token | HTTP 400 with validation response |
| View system roles | `GET /api/master/api-get-view-master-system-roles` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, system role list response |
| Add system role | `POST /api/master/api-post-add-update-master-system-role` | Valid system role payload, valid token | HTTP 200, DB response, system role records may be created |
| Missing system role fields | `POST /api/master/api-post-add-update-master-system-role` | Missing required ADD field, valid token | HTTP 400 with validation response |
| View survey types | `GET /api/master/api-get-view-master-survey-type` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, survey type list response |
| Add survey type | `POST /api/master/api-post-add-update-master-survey-type` | Valid survey type payload, valid token | HTTP 200, DB response, survey type records may be created |
| Missing survey type fields | `POST /api/master/api-post-add-update-master-survey-type` | Missing required ADD field, valid token | HTTP 400 with validation response |
| View property types | `GET /api/master/api-get-view-master-property-type` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, property type list response |
| Add property type | `POST /api/master/api-post-add-update-master-property-type` | Valid property type payload, valid token | HTTP 200, DB response, property type records may be created |
| Missing property type fields | `POST /api/master/api-post-add-update-master-property-type` | Missing required ADD field, valid token | HTTP 400 with validation response |
| View master details | `GET /api/master/api-get-view-master-details` | Valid token, optional `ITEM=VIEW_ALL` | HTTP 200, master details response |
| Health check | `GET /health` | No body | HTTP 200, API status JSON |
| DB unavailable | Any DB-backed API | RDS/network issue | HTTP 500 with error message |

## Module Ownership Boundaries

Backend owns:

- Route names and HTTP status mapping
- JWT token creation and validation
- Request payload normalization
- Stored procedure invocation
- Procedure response normalization
- User list and add/update payload handling
- Specific customer id validation
- Customer payload validation before DB call
- Specific project id validation before DB call
- Project payload validation before DB call
- Master system-role, survey-type, property-type, and master-details request validation before DB call

Database owns:

- Credential validation
- User list query rules
- User existence validation
- Specific customer profile query rules
- User insert/update rules
- Role and organization references
- Project insert/update rules
- System role insert/update rules
- Survey type insert/update rules
- Property type insert/update rules
- Master name/details query rules
- Stored procedure response messages

Frontend owns:

- Login form
- Token storage and protected route handling
- Add/update user form validation
- Add/update customer form validation
- Add/update project form validation
- Add/update system role form validation
- Add/update survey type form validation
- Add/update property type form validation
- Displaying backend and DB response messages

## Inactive Legacy Code

The following files are still present for reference but are not mounted in the
City Survey app entrypoint:

- `routes/event.js`
- `routes/attendeesportal.js`
- `routes/payment.js`
- `routes/fileuploaddownload.js`
- `routes/emailcontroller.js`
- `emailService/*`
- `config/payment/*`

Do not wire these back into `index.js` unless a confirmed City Survey
requirement needs them.
